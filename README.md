Purpose of useEffect?

useEffect is used to perform side effects in React components such as:

API calls
Updating the DOM
Setting timers
Saving data to localStorage

Explain Prop Drilling

Prop drilling occurs when data is passed from a parent component to deeply nested child components through multiple levels of props, even when intermediate components do not need that data
